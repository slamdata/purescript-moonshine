{
  "name": "purescript-moonshine",
  "private": true,
  "scripts": {
    "build": "pulp build -I test/src -- --censor-lib --strict --stash",
    "build:non-strict": "pulp build",
    "bundle": "purs bundle \"output/**/*.js\" --output ./index.js --main Main --module Main",
    "ide": "purs ide server",
    "start": "pulp run",
    "test": "pulp test"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "chalk": "^2.4.0",
    "pulp": "^12.0.1",
    "purescript": "^0.11.7",
    "purescript-psa": "^0.6.0",
    "xhr2": "^0.1.4"
  }
}
